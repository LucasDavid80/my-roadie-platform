name: My Roadie Ecosystem CI/CD

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  # ==========================================
  # LINTS JOBS
  # ==========================================
  frontend-lint:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend-web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: './frontend-web/package-lock.json'
      - run: npm ci
      - run: npm run lint
  
  backend-lint:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: './backend/package-lock.json'
      - run: npm ci
      - run: npm run lint
  # ==========================================
  # TEST JOBS
  # ==========================================
  # frontend-test:
  #   needs: frontend-lint
  #   runs-on: ubuntu-latest
  #   defaults:
  #     run:
  #       working-directory: ./frontend-web
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'
  #     - run: npm ci
  #     - run: npm test # Roda os testes (Jest/Vitest)

  # backend-test:
  #   needs: backend-lint
  #   runs-on: ubuntu-latest
  #   defaults:
  #     run:
  #       working-directory: ./backend
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'
  #     - run: npm ci
  #     - run: npx prisma generate
  #     - run: npm test # Roda os testes do NestJS

      
  # ==========================================
  # BUILD JOBS
  # ==========================================
  frontend-build:
    needs: frontend-lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend-web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: './frontend-web/package-lock.json'
      - run: npm ci
      - run: npm run build

  backend-build:
    needs: backend-lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: './backend/package-lock.json'
      - run: npm ci
      - run: npx prisma generate
      - run: npm run build

  # ==========================================
  # MOBILE JOBS (Comentado para economizar minutos)
  # ==========================================
  # mobile-build:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: subosito/flutter-action@v2
  #       with:
  #         flutter-version: '3.x'
  #     - run: flutter pub get
  #     - run: flutter build apk --release

  # ==========================================
  # DEPLOY JOBS (Placeholders para o futuro)
  # ==========================================
  # frontend-deploy:
  #   needs: frontend-build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Deploy to Vercel
  #       run: echo "Aqui entrará o comando de deploy da Vercel"

  # backend-deploy:
  #   needs: backend-build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Deploy to Render
  #       run: echo "Aqui entrará o webhook do Render"

  # notify:
  #   needs: [frontend-deploy, backend-deploy]
  #   runs-on: ubuntu-latest
  #   if: always()
  #   steps:
  #     - name: Send Notification
  #       run: echo "Pipeline finalizado com sucesso!"